<Window x:Class="WpfApp2.View.DeleteTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="DeleteTask" Height="150" Width="300">
    <Border Background="#eff2f7" CornerRadius="10">

        <Grid Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Do you want to delete?" FontSize="15" HorizontalAlignment="Center" Grid.Row="0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0" Grid.Row="1">
                <Button Style="{StaticResource ChooseOption}" Content="Yes" Width="75" Margin="5" Click="ConfirmButton_Click"  />
                <Button Style="{StaticResource ChooseOption}" Content="No" Width="75" Margin="5" Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>